Sesion 2:
    Adicion de Archivos
        - git Add (Nombre del Archivo conjuntamente con su extencion)
        - git Add *   (Adiciona todos los Archivos)
        - git add -A
        Estado del proyecto Como Se encuentra el estado del proyecto
            (commi, agrupacion, las ramas)
        - git status (Se puede Ver el estado)

        git commit (nos permite agrupar un conjunto de cambios)

        git commit -m "Mensaje para el Commit" Se un mensaje Breve  y

        Enlazar nuestro repositorio remoto al repositorio local
            -- git remote add origin (URL del repositorio)
            -- git remote -v // nos permite ver el repositorio remoto al que estamos enlazados.


            // Crear una Rama antes de entrar al repositorio remoto

            ssh-keygen -t rsa -b 4096 -C "morga.gary."

            git push --set-upstream origin master
            eval 

            git push --set-upstream origin master

            Navegar a un commit anterior (Cuidado a este comando)

            git reset --hard (id del commit al que quiero navegar)

            e9e04485423e81cf951356f4c2467ea1f27e30df



            Se va a hablar de ramas